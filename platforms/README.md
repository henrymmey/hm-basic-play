# Platform packages

The modpack has separate Packwiz roots for each distribution platform:

- `modrinth/` uses Modrinth metadata and produces `release.mrpack`.
- `curseforge/` uses CurseForge metadata where a compatible file exists and produces `release-curseforge.zip`.

Files without a compatible CurseForge release remain bundled in the CurseForge package.
